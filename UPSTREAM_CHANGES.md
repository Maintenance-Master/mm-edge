## .gitignore
Appended MM-owned ignore patterns (*.db, for the durable queue) to the
existing upstream .gitignore. Low-risk, administrative only — no runtime
behavior change, no expected merge conflict with future upstream releases.